# Database (MySQL) — Railway provides these via MYSQL_* variables
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=dtom

# JWT
JWT_SECRET=change-me-to-a-random-secret
JWT_EXPIRES_IN=7d

# App
PORT=3000

# Frontend URL for CORS
FRONTEND_URL=https://your-vercel-app.vercel.app
